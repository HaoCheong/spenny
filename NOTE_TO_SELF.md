# Problems

- When you run move_to_pi.sh it is still breaks. For some reason the backend cannot connect to the DATABASE
- Also it is using the staging env? It says so on the page (caching? no because I tested it with private mode)